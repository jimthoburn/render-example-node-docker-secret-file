services:
- type: web
  name: render-example-node-docker-secret-file
  plan: starter
  env: docker
  pullRequestPreviewsEnabled: false
  envVars:
  - key: PUBLIC_PORT
    value: 80
  - key: PUBLIC_MESSAGE
    value: "Hello world 👋"
